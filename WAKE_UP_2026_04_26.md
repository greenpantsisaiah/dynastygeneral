# Wake-Up Checklist for 2026-04-26

Overnight 2026-04-25 to 2026-04-26 ship summary. Read this top of session, then act in order.

## What shipped overnight (already on origin/main, Vercel auto-deployed)

| Commit | What | Files |
|---|---|---|
| cec9ee8 | Verdict imports canonical FormatRules from llm-contract | `lib/scout/score.ts`, `lib/scout/verdict.ts` |
| ccccdc6 | Cookie consent banner, env-gated (off by default) | `components/cookie-banner.tsx`, `app/layout.tsx` |
| 468d3b4 | **Soundboard scaffold**: 6 dials, /soundboard route, argue + suggest endpoints, 3 new Supabase tables | 13 files, +1223 lines |
| 1087887 | INVARIANTS pillar: Soundboard is additive, never a refactor | `INVARIANTS.md` |
| (next) | Founder admin view at /soundboard/admin + DEPLOYMENT.md updates + this file | various |

## Required actions (in order)

### 1. Run the Soundboard migration (REQUIRED for argue + suggest to work)

```bash
# Option A: Supabase CLI (preferred)
cd web
supabase db push

# Option B: paste 0006_soundboard.sql into the Supabase SQL editor
cat web/supabase/migrations/0006_soundboard.sql | pbcopy
# then paste into https://supabase.com/dashboard/project/_/sql/new
```

Until this runs:
- `/soundboard` page renders fine
- Dial values save to your cookie immediately. Cross-device DB save activates after the migration (only the `judgment_profiles` upsert silently skips, no breakage).
- Argue + Suggest endpoints return 500. The UI fails-soft (modal shows "queue not ready yet, logged in console") so it doesn't break, just degrades.

After migration, sanity-check by visiting `/soundboard`, submitting a test argument on any dial, then visiting `/soundboard/admin`. The argument should appear in the queue.

### 2. Verify Vercel env vars

Open Vercel → project → Settings → Environment Variables. Required:

| Variable | Status check |
|---|---|
| `ANTHROPIC_API_KEY` | set (verdict + coach degrade gracefully if missing, but degraded means no LLM) |
| `KV_REST_API_URL` + `KV_REST_API_TOKEN` (or `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN`) | set; without them rate limits + budget cap silently disable |
| `ADMIN_EMAILS` | should include `isaiah@nextupleader.com` so you can hit `/soundboard/admin` |
| `BETA_OPEN_MODE` | `true` for now (Pro features open to all signed-in users) |
| `NFL_DRAFT_WINDOW_START` + `NFL_DRAFT_WINDOW_END` | set if NFL Draft is in window |

Optional but recommended:
- `SENTRY_DSN` for error tracking
- `NEXT_PUBLIC_COOKIE_BANNER_ENABLED` flip to `"true"` only if EU/UK traffic appears
- Vercel Analytics enabled

### 3. Smoke test the Soundboard end-to-end

After steps 1 + 2:

- Visit `/soundboard` while signed in. Six dials render. "Pending wiring" badge on each (correct; they're stored, not yet read by the engine).
- Move the Horizon slider. "Save profile" persists.
- Click "Argue" on any dial. Pick a shape, type a comment, submit. Should show success.
- Visit `/soundboard/admin`. Your test argument appears in the queue.
- Submit a suggestion. Verify it appears too.

### 4. Live test Coach pushback (the SF QB scenario)

This needed a human in the browser. Pick a SF league, ask Coach a thin-roster QB question, push back ("but I already have Hurts"). Coach should hold the line if it has named-roster context confirming the user owns Hurts; should revise if Coach genuinely missed a fact. The 2026-04-25 Coach voice update enforces no-capitulation as a hard rule.

## Optional next moves (priority order)

1. **Wire the Horizon dial into the engine.** This is the load-bearing migration we explicitly held off on. Hoist `penalizeForConstraint` in `lib/strategy/decision-synthesis/synthesize.ts` to read `JudgmentProfile.dials.horizon` (with fallback to existing `windows.declared` during transition). Validate every consumer (DecisionCard, Coach, briefings). Repeat per dial after.
2. **Pre-synthesis Monte Carlo for counter-view.** Replaces v1 deterministic ADP-survival heuristic in `buildCounterView` with proper P(available) Monte Carlo. About 3 days. Out of safe overnight scope; needs supervision.
3. **Decision Tension strip.** Hub-level component for high-stakes disagreements. About 4 days. Not started.
4. **FantasyCalc commercial-use email.** Sent earlier; file the reply when it lands.
5. **Marketing screenshot refresh.** Browser session needed; current `/public/marketing/decision-card.png` may show pre-consolidation state.

## Skipped (correctly) overnight

- Engine wiring of Soundboard dials (you said fundamental; needs guided session)
- Monte Carlo (too risky unsupervised)
- Decision Tension strip (4-day feature)
- Marketing screenshots (no browser available)
- Vercel env verification (dashboard access required)
- Live Coach pushback test (human-in-browser required)

## Files touched overnight

- `src/lib/scout/score.ts` (FormatRules import)
- `src/lib/scout/verdict.ts` (FormatRules consumption)
- `src/components/cookie-banner.tsx` (NEW)
- `src/app/layout.tsx` (cookie banner mount)
- `src/lib/soundboard/types.ts` (NEW)
- `src/lib/soundboard/storage.ts` (NEW)
- `src/app/api/soundboard/profile/route.ts` (NEW)
- `src/app/api/soundboard/argue/route.ts` (NEW)
- `src/app/api/soundboard/suggest/route.ts` (NEW)
- `src/components/soundboard/dial.tsx` (NEW)
- `src/components/soundboard/argue-modal.tsx` (NEW)
- `src/components/soundboard/suggest-form.tsx` (NEW)
- `src/components/soundboard/panel.tsx` (NEW)
- `src/app/soundboard/page.tsx` (NEW)
- `src/app/soundboard/admin/page.tsx` (NEW)
- `src/components/nav/user-menu.tsx` (Soundboard menu link)
- `src/app/robots.ts` (disallow /soundboard, /leagues)
- `supabase/migrations/0006_soundboard.sql` (NEW)
- `INVARIANTS.md` (Soundboard pillar)
- `DEPLOYMENT.md` (cookie banner + migration line)
- `WAKE_UP_2026_04_26.md` (this file)

## Discipline followed overnight

- Build clean before every push. Zero warnings, zero errors.
- Each commit complete and reversible.
- No load-bearing refactors. Did not touch windows declaration. Did not touch `penalizeForConstraint`. Did not touch the LLM contract shape. Did not migrate any existing surface.
- Additive only. New files, new tables, new routes.
- Tooltip surfaces are truthful: every Soundboard dial is badged "Pending wiring" because none of them feed the engine yet.

Sleep well. Next session: run the migration, smoke test, then pick from the optional list.
