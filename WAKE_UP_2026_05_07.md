# Wake-up brief: 2026-05-07 morning (latest)

This brief was last updated overnight 2026-05-07. The original 2026-05-06 wake-up content is preserved at the bottom.

## TL;DR · what shipped overnight

After you signed off, the autonomous session shipped the trade analyzer wedge (your filed "screenshot-worthy" idea), real survival math, canonical-sources hardening, and the specific-pick target_kind. Test A signal extraction is running in the background (your new key works, ~50 RBs coded so far across 2022 with 2023+2024 still to come).

## What you need to do (ranked)

1. **Run migration 0011 in Supabase.** The shareable verdict feature requires a new table. The Share button will 500 until this runs.
   - Open Supabase dashboard for the Dynasty General project
   - SQL editor → paste contents of `supabase/migrations/0011_shared_trade_verdicts.sql`
   - Run. Verify: `select count(*) from public.shared_trade_verdicts;` returns 0.

2. **Eyeball the trade analyzer feature.** Visit any league `/leagues/[id]/trade?mode=incoming` or `?mode=outbound`, generate a verdict, click "Share verdict". Confirm: link copied, URL works in incognito, OG card renders correctly when pasted into Slack / iMessage / Discord (open the link and the unfurl should show the verdict badge + headline + accuracy claim).

3. **Check extractor progress.** `tail -50 /tmp/dg-extractor.log`. If still running: nothing to do. If complete: I'll have left a final summary line `=== ALL THREE YEARS COMPLETE ===`. Once done, you can run `npm run pipeline:v1 -- --season 2022 --format 1qb` to refresh v1 numbers with full coverage and we can revisit the bellcow horizon-gating call.

4. **Decide on engine v1 calibration.** Per yesterday's memory, you locked in option 2 (horizon-gate the bellcow boost). The implementation is queued behind v1 data; once full coverage lands and the regression confirms the 7-RB pattern, I can ship the horizon-gated boost.

5. **Optional: nuke `.env.local.save` backup.** It's nano's autosave from the key-rotation flow. Has the OLD (dead) key in it. Safe to delete: `rm "/Users/isaiahmcpeak/Claude Code Projects/DynastyGeneral/web/.env.local.save"`.

## What overnight shipped (5 commits, all on main + deployed)

### Commit `435eb95` · fix(decision): real survival math + canonical-sources hardening
- Survival pct now opponent-game-theory math (no more 50% on every lane)
- Bucket label re-derived from pct so text + badge can never disagree
- Canonical pick-resolution helper consolidates 3 implementations
- `CANONICAL_SOURCES.md` registers per-domain canonicals (auto-loads via AGENTS.md)
- 3 new lint rules in `evals/anti-patterns.test.ts` catch parallel-implementation regressions
- New `evals/canon-equivalence.test.ts` asserts canonical helpers match a reference

### Commit `ed0e906` · feat(trade): pick target_kind for outbound attack
- Third target option in trade form: attack a specific draft pick
- End-to-end: route schema + engine TradeOutboundInput + form dropdown + analytics

### Commit `edd934e` · feat(trade): screenshot-ready verdict + UX polish
- Calibration footer on every trade verdict result ("Engine v0 · 0.399 Spearman")
- Prominent "Copy verdict" button generates league-chat-ready text
- Form copy polished with realistic placeholders + clearer mode descriptions

### Commit `7c1f0c0` · feat(trade): shareable verdict URLs + OG unfurl card
- New table `public.shared_trade_verdicts` (migration 0011 must be run)
- POST `/api/shared-verdicts` creates a share (logged-in only, RLS-enforced)
- Public viewing page at `/t/{code}` renders the verdict + CTA card
- OG image at `/t/{code}/opengraph-image` for rich unfurls (1200x630, methodology baked in)
- "Share verdict" button on result card (auto-copies URL to clipboard)
- 365-day expiration, lazy cleanup on access, view counter
- Rate limits: 10 creates / 10 min, 60 views / 1 min

## Memory captured during the session

- `project_calibration_horizon_gate.md`: option 2 (horizon-gate bellcow) locked in with your three-mechanism reasoning (trade-out, RBBC dilution, OL collapse)
- INVARIANTS.md updated: pre-fix checklist now includes "check CANONICAL_SOURCES.md before writing helpers"
- INVARIANTS.md updated: "one trade-aware pick resolver" pinned

## Anthropic key rotation outcome

Old `.env.local` key was orphaned (different org, $0 balance). New key created in your Next Up Leaders org which has $38.34 + auto-refill enabled. Extraction is running cleanly against the new key. Files cleaned up:
- `.env.md` (TextEdit's accidental save) → renamed to `.env.local`
- `.env.md.backup-stray` (April relic) → preserved as backup, ignorable
- `.env.local.save` (nano autosave with dead key) → safe to delete when convenient

## Filed for next session (need your guidance before building)

- **Test B simulation harness** (VALIDATION_PLAN §6): 12 agent strategies + draft simulation engine + scoring against historical actuals. Multi-session build. Want me to scaffold the agent interface + one or two baseline agents (ADP autodraft, KTC-only) as a starting point?
- **Counter-offer auto-generation**: when a verdict says "counter," auto-generate the actual counter trade with structured proposal. ~2 hours of work; clear scope.
- **Standing-call lane card duplication**: in many scenarios the standing-call player equals the lean lane card winner. Small UX fix to deduplicate. ~30 min.
- **Trade verdict share dashboard**: list of verdicts the user has shared, view counts, delete buttons. ~1 hour.
- **OG image personalization**: include the user's team display name once we plumb it through (currently strips it for privacy default).

## Spend tracker (rough)

- Anthropic credits at session start: ~$38.34 in Next Up Leaders org (after key rotation)
- Signal extractor in flight: ~$25-30 expected for full Test A
- No other API calls of note overnight

---

# Original 2026-05-07 wake-up brief

(preserved below for context)

## TL;DR

**Headline finding:** Dynasty General engine v0 (no signal codes loaded) achieves Spearman 0.399 averaged across 2022-2024 dynasty backtests. Beats FantasyPros ECR (0.365), FantasyPros ADP (0.364), and KTC market consensus (0.346). Detail in `MODEL_CARD.md` section 9.7 and `data/scoreboard/scoreboard_v1.csv`.

**Why this matters:** This is the first measurable claim that justifies the project's existence. Even without signal codes, our position rubric scaffolding plus age curves provides measurable lift over consensus. v1 with signals (in progress overnight) should add more.

## Field note: v1 with partial signals shows REGRESSION (under-coverage caveat)

Tested the v1 pipeline with 7 RBs coded for 2022 (extractor stopped early at credit-balance limit). Result:

- v0 nosignals 2022: Spearman 0.3901
- v1 signals 2022 (7 of 10 RBs coded, plus 0 of 100 non-RBs): Spearman 0.3778

v1 is consistently SLIGHTLY WORSE than v0 across both 4-RB and 7-RB partial samples (0.376 → 0.378 → vs 0.390). The signal-driven boost to bellcow RBs is hurting correlation on the 3-year cumulative horizon.

Decision (made 2026-05-07): horizon-gate the bellcow boost (option 2). See memory `project_calibration_horizon_gate.md`. Implementation queued behind full v1 coverage.

## Files to glance at first

1. `MODEL_CARD.md` section 9.7: the headline result with proper caveats
2. `LEGAL_GUARDRAILS.md`: the publishing rules
3. `CANONICAL_SOURCES.md`: the per-domain canonicals registry (NEW, auto-loaded)
4. `data/scoreboard/scoreboard_v1.csv`: the artifact

## Spend tracker (cumulative across sessions)

- Signal extractor 2022 (10 RBs at $0.17 each): ~$1.70 actual on 2026-05-06
- Signal extractor 2023 + 2024 + 2022 cleanup: estimated ~$25-30 in 2026-05-07 overnight session (key rotation completed; running now)
- Total estimated: $30-35 of Anthropic credits across both sessions
